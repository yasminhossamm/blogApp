<h1>Edit Comment</h1>

<%= form_with(model: [@post, @comment], local: true) do |form| %>
  <div>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit "Update Comment" %>
  </div>
<% end %>

<%= link_to 'Back', @post %>
